apiVersion: v1
kind: ConfigMap
metadata:
  name: db-conf
data:
  DATABASE_YAML: |
    default: &default
      adapter: postgresql
      encoding: unicode
      host: postgresql.shared
      port: 5432
      username: postgresql
      database: consul_staging
