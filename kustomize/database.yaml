apiVersion: v1
kind: ConfigMap
metadata:
  name: db-conf
data:
  database.yml: |
    production:
      adapter: postgresql
      encoding: unicode
      host: postgresql.shared
      port: 5432
      username: postgresql
      database: consul_staging
